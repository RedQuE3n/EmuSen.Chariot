bin/
obj/
out/
*.user

# The fork arrived with 66 MB of build output and a SQLite file committed. The
# database was the TODO scaffold's, not Chariot's, and nothing here reads it.
*.db
*.db-shm
*.db-wal


# A folder feed, populated by `dotnet pack` in the EmuSen.Pegasus checkout,
# and present only while a core version is unpublished. NuGet.config says when
# it goes. Packages are build output and belong here no more than bin/ does.
local-packages/
